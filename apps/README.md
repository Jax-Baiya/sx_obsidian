# apps

Runtime-facing application surfaces live here.

Planned occupants in later phases (move-only + compat):
- `obsidian-plugin/`
- `web/`
- optional thin launch wrappers (`api`, `cli`)

Phase 1 note: this directory is scaffold-only; no existing entrypoint behavior is changed.
