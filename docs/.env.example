# SX Obsidian Media Control - Template
# Copy this to .env and adjust your paths

# Active Profile (CLI --profile overrides this)
SX_PROFILE=default

# VAULT PATHS
# format: VAULT_{profile}=/path/to/vault
VAULT_default=/path/to/your/obsidian/vault

# METADATA SOURCES
# format: CSV_{type}_{index or profile}=/path/to/file.csv
CSV_consolidated_1=/path/to/main_consolidated.csv
CSV_authors_default=/path/to/authors.csv
CSV_bookmarks_default=/path/to/bookmarks.csv

# DIRECTORY STRUCTURES (Relative to Vault Root)
DATA_DIR=data
DB_DIR=_db/media
LOG_DIR=_logs
REPORTS_DIR=reports
