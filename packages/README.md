# packages

Stable reusable Python packages will be grouped here in later phases.

Planned occupants:
- `sx/`
- `sx_db/`
- `sx_scheduler/`

Compatibility requirement:
- legacy import roots (`sx`, `sx_db`, `sx_scheduler`) must continue to work via shims/re-exports during migration.
